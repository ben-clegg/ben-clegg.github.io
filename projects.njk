---
layout: common.njk
title: Projects
---

<section class="section">
    <div class="container">

        <div class="columns">        
            <div class="column is-4">
                {%- for project in collections.projectEntry | reverse -%}
                    {%- if collections.projectEntry.indexOf(project) % 3 == 1-%}
                        {% include 'projectEntry.njk' %}
                    {%- endif -%}
                {%- endfor -%}
            </div>
            <div class="column is-4">
                {%- for project in collections.projectEntry | reverse -%}
                    {%- if collections.projectEntry.indexOf(project) % 3 == 0-%}
                        {% include 'projectEntry.njk' %}
                    {%- endif -%}
                {%- endfor -%}
            </div>
            <div class="column is-4">
                {%- for project in collections.projectEntry | reverse -%}
                    {%- if collections.projectEntry.indexOf(project) % 3 == 2-%}
                        {% include 'projectEntry.njk' %}
                    {%- endif -%}
                {%- endfor -%}
            </div>
        </div>
    </div>
</section>
